{
    let name = `Maykel`
    let names = [`Maykel`, `Vitória`, `Miguel`]

    for(let caractere of name) {
        console.log(caractere)
    }
}
{
    let name = `Maykel`
    let names = [`Maykel`, `Vitória`, `Miguel`]

    for (let name of names) {
        console.log(name)
    }
}