// concatenando variáveis
{
let name, age, color
name = `maykel`
age = 27
color = `green`

console.log(`Meu nome é ` + name + ` tenho ` + age + ` anos, e minha cor preferida é ` + color)
}

// interpolando variáveis
{
let name, age, color
name = `maykel`
age = 27
color = `green`

console.log(`Meu nome é ${name} tenho ${age} anos e minha cor preferida é ${color}`)
}